"""General kernel-fusion area-distribution analyses.

Each subpackage models one fusion's custom latency-aware roofline+Snowcat kernel and
runs the die-area distribution over the full GLM-5.2 decode layer with only that kernel
fused. See ``notes/general_fusion_analysis.md``.
"""
